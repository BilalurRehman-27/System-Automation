import { connect } from "react-redux";
import CaseDetailTabs from "./CaseDetailTabs";

const mapStateToProps = (state: any) => {
  //return your store data here 
  return {};
};

export default connect(mapStateToProps, {})(CaseDetailTabs);
